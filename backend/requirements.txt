# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6  # For file uploads
pydantic>=2.0.0

# Image processing
Pillow>=10.0.0
pillow-heif>=0.16.0  # HEIC/HEIF support for iPhone photos

# PDF rendering (choose one or both)
weasyprint>=60.0  # HTML/CSS to PDF - requires system dependencies
# reportlab>=4.0.0  # Alternative PDF generation

# Future: Database
sqlalchemy>=2.0.0
# alembic>=1.12.0

# Map rendering
staticmap==0.5.5

# Env loading
python-dotenv>=1.0.0

# HTTP caching for map tiles (fallback when staticmap tile_cache unsupported)
requests-cache>=1.2.0

# Testing / HTTP client
httpx>=0.27.0

# Optional: thumbnail generation (install to enable PDF->PNG thumbnails)
# PyMuPDF is recommended for fast, pure-Python PDF page rendering on Windows/Linux
pymupdf>=1.22.0

# Future: S3 storage
# boto3>=1.28.0
